// SPDX-License-Identifier: GPL-3.0-or-later

/**
 * @file schema/domain/utils/index.ts
 * @description
 * Barrel for domain schema utilities.
 *
 * Curates and re‑exports helpers used across domain schemas,
 * such as combinators, shape mergers, and optional wrappers.
 *
 * Consumers should import from this file rather than deep paths.
 */

export * from "./schemaHelpers";
