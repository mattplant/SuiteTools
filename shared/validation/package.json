{
    "name": "@shared/validation",
    "version": "0.1.0",
    "main": "dist/index.js",
    "types": "dist/index.d.ts",
    "private": true,
    "peerDependencies": {
        "zod": "^4.0.13"
    },
    "devDependencies": {
        "zod": "^4.0.13"
    },
    "scripts": {
        "build": "tsc -b",
        "clean-local": "rm -rf dist"
    }
}
