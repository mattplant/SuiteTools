The following software may be included in this product: flowbite-react.

- **Declared licence:** `MIT` — the value of the `license` field in the package's own `package.json`.
- **Version examined:** 0.12.17
- **Source:** <https://github.com/themesberg/flowbite-react>
- **Homepage:** <https://flowbite-react.com>

**No licence text is reproduced here, because the package does not ship any.**
`flowbite-react@0.12.17` contains no `LICENSE`, `LICENCE`, `NOTICE` or `COPYING` file, so
`yarn licenses generate-disclaimer` omits it entirely and there is no upstream notice to
carry forward. The package's README links to the project's licensing page at
<https://flowbite.com/docs/getting-started/license/>.

The declared identifier above is recorded verbatim from the package. No copyright holder
is stated because upstream does not state one, and the full MIT terms are not reproduced
because upstream did not ship them — asserting either would put unverified text into a
document whose only purpose is to state licence terms accurately.

This entry exists to make the gap visible rather than to close it. Closing it requires
upstream to ship a licence file; see `docs/guides/third-party-licenses.md`.
